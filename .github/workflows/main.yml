name: Token Check
on: workflow_dispatch
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Token Length: ${#GITHUB_TOKEN}"
          echo "Token Start: $(echo $GITHUB_TOKEN | cut -d'.' -f1)"
          echo "Dots found: $(echo -n $GITHUB_TOKEN | tr -cd '.' | wc -c)"
